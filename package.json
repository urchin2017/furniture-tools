{
  "name": "furniture-tools",
  "version": "0.0.0",
  "private": true,
  "description": "家具出口内部工具 monorepo（前端 apps/web + 后端 apps/api + shared）",
  "workspaces": [
    "apps/web",
    "shared/ts"
  ],
  "engines": {
    "node": ">=20"
  }
}
